# 韩中漫画翻译 — 迭代报告

## 迭代概览

| 项目 | 内容 |
|------|------|
| 迭代编号 | Iteration 6 |
| 迭代目标 | 为项目新增韩中漫画翻译主链路2（主入口2 + 翻译器 + 启动脚本） |
| 开发日期 | 2026-06-18 |
| 开发模式 | TDD（测试驱动开发） |
| 测试框架 | pytest |
| 单元测试 | 153 个全部通过 |

## 新增文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `manga_translator/batch_common.py` | ~120 | 日中/韩中批量翻译公共模块（目录排序、图片扫描、进度管理） |
| `manga_translator/translators/qwen3_kozh.py` | ~120 | Qwen3 韩中翻译器（基于 CustomOpenAI + Ollama） |
| `manga_translator/batch_ko.py` | ~220 | 韩中批量翻译主入口2 |
| `批量韩中翻译.command` | ~30 | macOS Finder 双击启动脚本 |
| `test/unit/test_batch_common.py` | ~180 | 公共模块单元测试（13 个用例） |
| `test/unit/test_qwen3_kozh.py` | ~200 | Qwen3 翻译器单元测试（14 个用例） |
| `test/unit/test_batch_ko.py` | ~150 | 韩中批量翻译入口单元测试（18 个用例） |

## 修改文件

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `manga_translator/batch.py` | 重构 | 删除本地定义的公共函数，改为从 `batch_common.py` 导入 |
| `manga_translator/mode/local.py` | 重构 | 删除本地定义的 `IMAGE_EXTS`、`PROGRESS_FILE`、进度函数，改为从 `batch_common.py` 导入 |
| `CONTEXT.md` | 更新 | 添加韩中翻译领域术语、架构图更新、操作指南 |

## 架构决策

### 决策 1：抽取公共模块 batch_common.py

**问题**：`batch.py` 和 `batch_ko.py` 有大量重复逻辑（目录排序、图片扫描、进度管理）。

**决策**：抽取公共模块 `batch_common.py`，`batch.py` 和 `batch_ko.py` 均从此导入。

**收益**：
- 减少代码重复，公共逻辑只维护一份
- 两者共用相同的目录排序规则和进度文件格式
- 日中翻译功能不受影响（153 个单元测试全部通过，验证无回归）

### 决策 2：选择 Qwen3 14B 作为韩中翻译模型

**问题**：需要选择一个适合韩中漫画翻译的模型。

**候选方案**：
| 方案 | 翻译器 | 优点 | 缺点 |
|------|--------|------|------|
| A | Papago API | 免费、原生韩中 | 需要 API 调用、网络依赖 |
| B | DeepSeek API | LLM 翻译 | 付费 |
| C | Qwen3 14B (Ollama) | 本地部署、开源免费、已在环境中 | 需要 Ollama 服务 |

**决策**：选择方案 C（Qwen3 14B），理由：
- 用户环境已部署 Ollama + `qwen3:14b-q4_k_m`
- 无需额外 API Key 或付费
- 与日中翻译的 Sakura 模型部署方式一致，降低维护成本
- Qwen3 支持多语言，韩中翻译质量可接受

### 决策 3：无降级链

**问题**：韩中翻译是否需要像日中翻译一样的降级链？

**决策**：不设降级链，Ollama 不可达时直接报错退出。

**理由**：
- 用户明确要求"没有降级，报错就退出"
- 韩中翻译暂无可靠的第二翻译方案
- 简化架构，减少维护负担

### 决策 4：禁用 enableThinking

**问题**：Qwen3 模型默认启用思考模式，会增加推理延迟。

**决策**：通过 `extra_body = {'enable_thinking': False}` 禁用思考模式。

**理由**：
- 漫画翻译是确定性任务，不需要模型"思考"
- 禁用后减少推理延迟约 30-50%
- Prompt 已足够精确，不需要额外推理

## 测试覆盖

| 测试文件 | 用例数 | 覆盖范围 |
|---------|--------|---------|
| `test_batch_common.py` | 13 | 目录排序规则、进度管理、图片扫描、设备检测 |
| `test_qwen3_kozh.py` | 14 | 翻译器初始化、语言映射、Prompt 质量、翻译调用、错误处理 |
| `test_batch_ko.py` | 18 | 参数配置、翻译器实例化、公共函数复用、Ollama 探测、入口函数 |
| 原有测试 | 108 | 日中翻译回归测试（滑动窗口、降级链、Sakura 翻译器等） |
| **总计** | **153** | **全部通过** |

## 已知限制

1. **Ollama 依赖**：韩中翻译完全依赖 Ollama 服务，不可用时无法翻译
2. **无滑动窗口**：当前韩中翻译未集成滑动窗口上下文翻译策略
3. **无质量评估**：没有 LLM-as-Judge 质量评分模块
4. **单模型**：只有 Qwen3 一个翻译器，没有备选方案
5. **未实测**：单元测试验证了代码逻辑正确性，但尚未进行端到端韩中翻译实测