# 韩中漫画翻译 — 性能实测报告

## 测试环境

| 项目 | 内容 |
|------|------|
| 硬件 | Apple M4 Pro / 64GB 统一内存 / macOS |
| Python 3.11.5 |
| Ollama 服务 | localhost:11434 (v0.22.0) |
| 翻译模型 | qwen3:14b-q4_k_m (Q4_K_M 量化) |
| 翻译器 | Qwen3KoZhTranslator (think=false, 原生 /api/chat) |
| 检测模型 | RT-DETR-V2 (MPS) |
| OCR 模型 | manga-ocr-base (MPS) |
| 擦除模型 | LaMa Large (MPS) |
| 源语言 | 韩文 (ko) |
| 目标语言 | 简体中文 (CHS) |
| 测试数据 | 60 张 webp 漫画页面（约 1000px 宽） |

## 端到端翻译实测（修复后）

手动测试 10 页（2026-06-18，~1000px WebP，无 benchmark 系统，仅墙钟计时）：

| 统计 | 值 |
|------|-----|
| 平均 | **~6.8s/页** |
| 最小 | ~4.7s |
| 最大 | ~10.3s |
| 吞吐量 | **~8.8 页/分钟** |
| 总耗时 | ~68.2s (10 页) |

> 注：第 1-2 页较慢（9-10s）因模型首次加载/预热，后续页面稳定在 5-7s。batch_ko.py 未集成 benchmark 系统，逐页明细和文字块数为手动记录，精度有限。

## 阶段耗时分析

`batch_ko.py` 未集成 benchmark 管道，无法精确分解管线各阶段耗时。翻译阶段可通过 `_request_translation()` 内部的 `[Qwen3KoZh TIMING]` 日志追踪 HTTP 请求耗时（build → http → parse），实测翻译 API 调用耗时约 **0.6s–2.9s**（修复前 15-27s）。其余阶段（Detection、OCR、Inpainting、Render）在 `mode/local.py` 中均无阶段级计时输出。

> 注：如需逐管线阶段的精确数据，需为 `batch_ko.py` 接入 benchmark 系统。日中翻译（`batch.py`）的 benchmark 管道可参考 `batch.py:249-266` 处实现。

## 性能修复前后对比

| 指标 | 修复前 | 修复后 | 提升 |
|------|--------|--------|------|
| 翻译 API 调用 | 15-27s | 0.6-2.9s | **~10x** |
| 单页翻译 | 28.7s | 6.8s | **4.2x** |
| 页/分钟 | 2.1 | 8.8 | **4.2x** |

### 修复前性能瓶颈分析

修复前 Qwen3 模型每次翻译请求会生成 200-300 个思考 token：
- 思考 token 生成：约 10-12s（占 API 总耗时的 80%+）
- 实际翻译 token 生成：约 2-3s（仅 2-3 个 token）
- 总 API 调用：15-27s/次

根因：`/v1/chat/completions`（OpenAI 兼容端点）不支持 `think` 或 `enable_thinking` 参数，模型始终以思考模式运行。

### 修复方案

将 `Qwen3KoZhTranslator._request_translation` 从 OpenAI 兼容端点改为 Ollama 原生 `/api/chat` 端点，直接传 `"think": false`：

```python
payload = {
    'model': self.model,
    'messages': messages,
    'stream': False,
    'think': False,  # 禁用思考模式
    'options': {
        'temperature': self.temperature,
        'top_p': self.top_p,
        'num_predict': self._MAX_TOKENS // 2,
    },
}
async with aiohttp.ClientSession() as session:
    async with session.post(self._ollama_chat_url, json=payload) as resp:
        data = await resp.json()
```

## 单元测试性能

| 测试文件 | 用例数 | 耗时 | 平均/用例 |
|---------|--------|------|-----------|
| `test_batch_common.py` | 28 | ~0.5s | 0.02s |
| `test_qwen3_kozh.py` | 14 | ~6.2s | 0.44s |
| `test_batch_ko.py` | 18 | ~3.7s | 0.20s |
| 全部单元测试 | 188 | ~10.4s | 0.06s |

> 注：`test_qwen3_kozh.py` 中包含 2 个实际 Ollama API 调用测试（翻译测试），其余均为纯单元测试。

## 优化措施

### 1. 禁用 thinking 模式（关键优化）

- **效果**：API 调用从 15-27s 降至 0.6-2.9s，约 10x 提速
- **实现**：使用 Ollama 原生 `/api/chat` 端点 + `"think": false`
- **质量影响**：无明显影响（漫画翻译为确定性任务，不需要推理链）
- **局限性**：`/v1/chat/completions`（OpenAI 兼容端点）不支持此参数，必须使用原生端点

### 2. 温度参数优化

- **temperature = 0.3**：降低随机性，提高翻译一致性
- **top_p = 0.3**：限制采样范围，减少翻译偏差

### 3. 模型单次加载

- 模型加载一次（约 0.7s），整个批次复用
- 避免逐页重复加载开销

### 4. 进度续传

- 每页翻译完成后立即写入进度文件
- 中断后重启自动跳过已翻译页面

## 与日中翻译对比

同批次横向对比见下方「全链路性能复测 — 与日中翻译横向对比」章节。旧测试因图片尺寸不同（1000px WebP vs 2048px JPG），不宜直接对比总耗时。

| 维度 | 日中翻译 (Sakura 14B) | 韩中翻译 (Qwen3 14B) |
|------|----------------------|---------------------|
| 翻译模型 | sakura-14b-qwen2.5-v1.0 | qwen3:14b-q4_k_m |
| 部署方式 | GGUF 本地 + Ollama HTTP | Ollama HTTP (原生 /api/chat) |
| 降级链 | 方式B(GGUF) → 方式A(Ollama) | 无降级 |
| 质量评估 | LLM-as-Judge 已集成 | 未集成 |
| 滑动窗口 | 已集成 | 未集成 |
| 模型特化 | 仅日→中 | 多语言（含韩→中） |
| Benchmark 支持 | 有 | 无 |

## 结论

- 韩中翻译链路完整可用。旧测试（1000px WebP）端到端平均 ~6.8s/页；新测试（2048px JPG，相同图片集统一基准）全量 110.1s/10页（11.01s/页），详见下方复测章节
- 与方式A (Sakura Ollama HTTP) 性能持平（同一批 2048px 图片差异 <5%），GGUF 直连仍显著领先
- 关键优化：禁用 Qwen3 thinking 模式，API 调用提速 ~10x
- 教训：Ollama 的 OpenAI 兼容端点不完全兼容，模型特定参数（如 `think`）需使用原生端点
- 局限：`batch_ko.py` 未集成 benchmark 系统，无法获得逐管线阶段耗时分解

## 全链路性能复测 (2026-07-05)

### 测试环境

| 项目 | 配置 |
|------|------|
| 测试日期 | 2026-07-05 |
| 设备 | Apple Silicon Mac (统一内存 64GB) |
| GPU 后端 | MPS (Metal Performance Shaders) |
| 测试图片 | 10 张日文漫画原图 (2048px) + 2 张续传验证 |
| 翻译模型 | qwen3:14b-q4_k_m (Ollama, 原生 /api/chat) |
| 翻译器 | Qwen3KoZhTranslator (think=false) |

> 注：本次测试使用与日中翻译相同的 2048px 日文漫画原图，以统一基准。旧测试使用 1000px WebP 图。图片尺寸和文本复杂度差异导致本次耗时高于旧数据。

### 全量翻译 (retrans=true, 10 页)

| 指标 | 值 |
|------|-----|
| 总耗时 | 110.1s (1m 50s) |
| 平均每页 | 11.01s |
| 吞吐量 | 5.45 页/分钟 |
| 成功页数 | 10/10 |

### 续传翻译验证 (retrans=false, 原10页 + 新增2页)

| 指标 | 值 | 目标 |
|------|-----|------|
| 续传耗时 | 18.8s | 仅翻译新图 |
| 实际翻译页数 | 2 | 2 |
| 汉化目录图片数 | 12 | 12 |
| Progress 记录 | 12 | 12 |
| 验证结果 | PASS | — |

### 与日中翻译横向对比 (10页, 2026-07-05)

| 指标 | 韩中 (Qwen3 Ollama) | 方式A (Sakura Ollama) | 方式B (Sakura GGUF) | 方式C (Galtransl GGUF) |
|------|--------------------|---------------------|-------------------|---------------------|
| 总耗时 | 110.1s | 105.3s | 79.7s | 80.9s |
| 平均每页 | 11.01s | 10.53s | 7.97s | 8.09s |
| 吞吐量 | 5.45 页/分钟 | 5.70 页/分钟 | 7.53 页/分钟 | 7.42 页/分钟 |
| 续传耗时 | 18.8s | 34.3s | 17.3s | 17.5s |

> 韩中翻译（Qwen3 Ollama）与方式A（Sakura Ollama）同为 Ollama HTTP 模式，性能接近（110.1s vs 105.3s，差异 4.6%）。GGUF 直连模式（方式B/C）仍保持显著优势。

### 结论

1. **韩中翻译全链路稳定**：全量+续传均通过，progress 机制正确
2. **与方式A (Sakura Ollama) 性能持平**：同一批图片，同为 Ollama HTTP 协议，差异 <5%
3. **续传机制正常**：正确跳过已完成页面，仅翻译 2 张新图
4. **GGUF 直连仍是最优方案**：方式B/C 比 Ollama HTTP 模式快约 25%